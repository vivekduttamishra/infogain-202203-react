
import ReactDOM from 'react-dom'; //standard components are always imported without path ./

import component from './component'; //our own component should be imported with relative ./ or ../



ReactDOM.render(component, document.getElementById("placeholder"));